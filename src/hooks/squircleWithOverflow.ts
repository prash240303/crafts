'use client';

// Re-export cornerkit's hook for imperative squircle usage.
// For the declarative component with overflow clipping, use SquircleWithOverflow from components/ui.
export { useSquircle } from '@cornerkit/react';
